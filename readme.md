# statistics API
- Install python
  - ```apt install python```
- Goto API directory
- Install requirements:
  - ```pip install -r requirements.txt```
- Run unicron:
  - ```unicorn main:app --reload```